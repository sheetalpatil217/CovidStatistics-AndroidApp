<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorAppBar</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/colorAccent</item>
<!--        <item name="actionBarStyle">@style/Widget.AppTheme.ActionBar</item>-->
    </style>

    <style name="Widget.AppTheme.ActionBar" parent="Widget.AppCompat.ActionBar">
        <!-- title text color -->
        <item name="titleTextColor">@color/textBar</item>
        <!-- subtitle text color -->
        <item name="subtitleTextColor">?android:textColorSecondary</item>
    </style>

    <style name="TextAppearance.AppCompat.Widget.ActionBar.Title" parent="@style/TextAppearance.AppCompat">
        <!-- title text color -->
        <item name="android:textColor">@color/iconColor</item>
        <item name="android:textSize">20dp</item>
        <!-- subtitle text color -->
<!--        <item name="subtitleTextColor">?android:textColorSecondary</item>-->
    </style>


</resources>
